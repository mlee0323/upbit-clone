# Database connection string (use postgresql+psycopg for psycopg3)
DATABASE_URL=postgresql+psycopg://postgres:postgres@timescaledb:5432/upbit_clone

# Note: Upbit public ticker API does not require authentication
# API keys are only needed for private endpoints (wallet, orders, etc.)

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://frontend:80
